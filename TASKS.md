# Tasks

> Generated by the orchestration run on 2026-06-24. Each task has a self-contained
> doc under `docs/tasks/<id>_*.md`. Dependency graph + wave plan:
> `docs/tasks/_PLAN.md`. **Execution is NOT started** — awaiting human review/approval
> of the parallel waves below. The gated task (G1) requires explicit confirmation.
>
> **Decisions (2026-06-24, human-confirmed):**
> 1. **Package manager = `pnpm`** — `pnpm check` is the canonical blog verify; `bunx` is a local fallback only.
> 2. **Rust lane = one builder, sequential** — `R1 → R2 → R3 → R4 → R5` are owned by a single builder (they share `page/mod.rs`); the other wave-1 tasks run in parallel beside it.
> 3. **D1 added** (wave 1) — Drawio/Mermaid `Latex` delivery decision.

## Active

### Wave 1 — ✅ COMPLETE (2026-06-24, builder agents; verified by orchestrator)
- [x] **B1_text_flat_marks** — `text.svelte` reads flat `marks` array. ✓ `pnpm check`: no new errors from this file.
- [x] **B2_code_fields_lang** — `code.svelte` maps `fields.lang` → svelte-highlight language (fallback typescript). ✓
- [x] **B4_repoint_generateor_alias** — `$generateor` → `src/types` in `svelte.config.js`. ✓ new types resolve; no module/type errors.
- [x] **P0_stabilize_scaffold** — moved to `src/` layout, added `pandas` dep, `cli.py main()`, deleted broken `data_cell.py`/empty `exec_cell.py`, `runner.py` + 13 real tests. ✓ `uv run pytest`: **18 passed**.
- [x] **D1_drawio_mermaid_latex_delivery** — decision written (`docs/decisions/drawio-mermaid-latex.md`). Most processors OK as-is; only Drawio structurally broken. Spun off **D1-T1** (Rust) + **D1-T2** (blog). No code changed.
- _Rust lane (single builder, sequential) — ✓ `cargo build` + `cargo test`: 7 passed (4 new):_
  - [x] **R1_emit_coverimage** — `cover_image: Option<String>` → `coverImage`, from first image in `page/util.rs`. ⚠️ emits `files/<name>` (see X1 caveat).
  - [x] **R2_emit_description** — `description: Option<String>` wired from existing `page/attr.rs` parse.
  - [x] **R3_dual_asset_copy** — new `--static-assets-path` flag (default `static/blog/assets`), copies alongside export.
  - [x] **R4_customcomponent_decision** — **DECISION: drop/defer** (`docs/decisions/customcomponent.md`). No impl.
  - [~] **R5_user_workspace_resolution** — **DEFERRED** (no consumer needs resolved author/workspace today).

### Wave 2 (depend only on wave-1 tasks)
- [ ] **B3_toggle_leveltext_items** — blog: render Toggle/list `LevelText` items in `text.svelte`. deps: B1 · blocks: — · wave 2
- [ ] **X1_blog_consume_coverimage** — blog: consume `coverImage` (OG image) + type. deps: R1 (DONE) · blocks: — · wave 2
  - ✅ **Path form resolved (2026-06-25):** R1 now emits `coverImage` = bare `<name>` (the blog prepends `/blog/assets/files/`). `coverImage` + `description` added to `src/types/page.ts`. X1 remaining work = confirm `+page.server.ts` OG read + any blog UI use of the cover image.
- [ ] **X2_verify_asset_path_serving** — blog: verify/fix asset serving from `static/blog/assets/`. deps: R3 · blocks: — · wave 2
- [ ] **P1_core_table_schema** — Python: Phase 1 execute→capture→Table-Schema envelope. deps: P0 · blocks: P2, P3 · wave 2

### Wave 3 (depend on wave-2 tasks)
- [ ] **P2_blog_integration_standalone** — Python+blog: Phase 2 standalone `.viz.json` + blog "Data" tab. deps: P1 · blocks: P4 · wave 3
- [ ] **P3_preprocessor_convert_blog_post** — Python+Rust+blog: Phase 3 sidecar pre-processor + Rust `data_tables` + blog render. deps: P1 · blocks: P4 · wave 3

### Wave 4 (depend on wave-3 tasks)
- [ ] **P4_polish** — Python: Phase 4 polish (README, CI, optional caching). deps: P2, P3 · blocks: — · wave 4

## Waiting On
- [x] **G1_remove_convert_blog_post_ts** — ✅ **DONE (2026-06-25, human-confirmed)** — `tools/convert_blog_post_ts/` removed. Verified: `pnpm check` shows the same 23 pre-existing errors (no new ones), `cargo test` green. Blog resolves types via `$generateor`→`src/types`.

## Someday

_Follow-ups surfaced during Wave 1:_
- [ ] **D1-T1** — Rust: investigate Drawio companion-file linkage in the `.pb` block tree; if a sibling `File` block carries the diagram, add `file_url` to `LatexComponentAttr` + wire asset copy; else document Drawio as unrecoverable. (see `docs/decisions/drawio-mermaid-latex.md`)
- [ ] **D1-T2** — blog: add `latex.svelte` branches for `Latex` (KaTeX math), `GithubGist`, `Drawio` (img/placeholder, after D1-T1), and an unknown-processor `{:else}` fallback.
- [ ] **CLEANUP-blog-typecheck** — resolve the ~23 pre-existing `svelte-check` errors (missing `$assets/*?format=...` module decls, card `title_slot`/`content_slot`/`serie` props, stray `key`/`hx-get`/`open` attrs, `ui/tabs/*`, implicit-any route params). Not caused by Wave 1.
- [ ] **DOC-r3-static-path** — document `--static-assets-path` (relative to CWD; pass absolute path if not run from repo root) in the tool README/CLAUDE.md.
- _(coverImage path-form reconciliation folded into X1 above.)_

## Done
